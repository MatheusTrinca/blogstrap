<%= render 'longer_featured' %>

<div class="row mb-2">
  <%= render 'medium_featured' %>
  <%= render 'medium_featured' %>
</div>

<div class="row g-5">
  <div class="col-md-8">
    <h3 class="pb-4 mb-4 font-italic border-bottom">
      From the Firehose
    </h3>
    <%= link_to 'New article', new_article_path %>  

    <%# <% @articles.each do |article| %>
      <%# <%= render article %>
    <%# <% end %> 

    <%# Porque tem o mesmo nome "articles / _article" %>

    <%= render @articles %>

    <%= render 'pagination' %>
  </div>

  <%= render 'right_side' %>

<div>
